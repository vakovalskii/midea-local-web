# Скопируйте в .env и заполните.

# Токен доступа к API. Длинный случайный (например: openssl rand -hex 32).
# Без него /api/* открыты всем, кто достучится до порта — для публикации в
# интернет ОБЯЗАТЕЛЕН.
AC_API_TOKEN=replace-with-long-random-token
